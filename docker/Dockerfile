# Dockerfile (Phase 0 scaffold).
# Listed and structured, not built in Phase 0. Do not run docker build yet.
# Phase 1 builds the app image; the pgvector-enabled Postgres comes from the
# compose file alongside it.

FROM python:3.12-slim

WORKDIR /app

# Phase 1: copy project metadata and install the listed dependencies.
# COPY pyproject.toml ./
# RUN pip install --no-cache-dir .

# Phase 1: copy source.
# COPY src/ ./src/

# Phase 1: default command runs one synthetic sample through the flow.
# CMD ["python", "-m", "ai_ops_workflow.cli", "--config", "demo_quarry", \
#      "--provider", "mock"]
