"""Pytest fixtures (Phase 0 placeholder).

Phase 1 will provide fixtures for the mock provider, a test pgvector store (or a
fake), and the demo_quarry config, so the full graph can be exercised offline.

No fixtures defined in Phase 0.
"""
