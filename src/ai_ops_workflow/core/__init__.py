"""Domain-agnostic workflow core.

Contains the LangGraph flow, the typed graph state, the audit log, the config
loader, and base models. Nothing in this package references a specific domain;
domain specifics are injected via a WorkflowConfig (see configs/).

Phase 0: placeholder modules only.
"""
